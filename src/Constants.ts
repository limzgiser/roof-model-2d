const DATA_COUNT = 5

const LABEL_OFFSET_Y = 3        // 标注距离标注线的偏移量

const THINKNESS = 15            // 标注线的厚度

const MOVE_X_TIMES = 3          // X 方向避让检测次数 正逆向分别 MOVE_X_TIMES 次

const MOVE_Y_TIMES = 10        // Y 方向避让检测次数 , 每次步长，字体高度的1.2 倍

const FONST_SIZE = 18

const LABEL_LINE_COLOR = '#32CD32'

const LABEL_TEXT_COLOR = '#000'

export {
    DATA_COUNT,
    LABEL_OFFSET_Y,
    THINKNESS,
    MOVE_X_TIMES,

    FONST_SIZE,
    LABEL_LINE_COLOR,
    LABEL_TEXT_COLOR,

    MOVE_Y_TIMES
}